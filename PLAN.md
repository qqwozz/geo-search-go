Отлично! Убираем монетизацию, API-ключи, балансы и всю B2B-инфраструктуру. Переориентируем проект на **B2C-продукт** — бесплатный, удобный, с быстрым доступом через веб-интерфейс или Telegram-бота.

---

## 1. НОВАЯ СУТЬ ПРОЕКТА (B2C-версия)

Вы делаете **умный геопоисковик для обычных людей**. Не для бизнеса, а для жителей города, которые хотят быстро найти место под свои желания.

**Примеры использования:**
- *"Где поесть вкусный завтрак рядом с работой?"*
- *"Найди кафе с террасой и вайфаем, чтобы поработать"*
- *"Где выпить кофе и не шумно?"*
- *"Место для свидания с красивым видом"*

**Ценность для пользователя:** 
- Не нужно тыкать в 10 фильтров.
- Понимает разговорный язык.
- Выдает не просто список, а **рекомендации с объяснением** (*"Это место подходит, потому что..."*).

**Монетизация (скрытая, но честная):**
- Вы не берете деньги с пользователей, а зарабатываете на **партнерских интеграциях** (например, заведение платит, чтобы его поднимали выше в выдаче).
- Показываете **рекламные карточки** мест, которые готовы платить за продвижение.

---

## 2. АРХИТЕКТУРА (B2C-УПРОЩЕНИЕ)

Убираем слои, связанные с биллингом и API-ключами. Остается 3 сервиса:

| Слой | Название | Задача | Технология |
| :--- | :--- | :--- | :--- |
| **1** | Фронтенд (Web/Telegram) | Показывает форму ввода и результаты поиска | React / Telegram Bot API |
| **2** | Бэкенд-ядро (Go) | Принимает запросы, вызывает NLP и Geo, возвращает ответ | Go (один сервис) |
| **3** | NLP-модуль (Python) | Парсит текст в фильтры | Python (FastAPI) |
| **4** | Хранилище (PostgreSQL + Redis) | Хранит POI, кеширует запросы | PostGIS, Redis |

**Упрощение:** Теперь Go-сервис не проверяет API-ключи и не списывает баланс. Он просто принимает запрос от фронтенда, делает всю логику и возвращает результат. **Никакой авторизации** (кроме базовой защиты от спама).

---

## 3. ПОТОК ЗАПРОСА (ЧТО ПРОИСХОДИТ)

### Шаг 1. Пользователь вводит запрос
- **Веб-версия:** Поле ввода на главной странице (как в Google).
- **Telegram-бот:** Пользователь пишет сообщение боту.

### Шаг 2. Бэкенд (Go) принимает запрос
- Проверяет, что текст не пустой и не слишком длинный (до 500 символов).
- Определяет город пользователя (через IP-геолокацию или если пользователь явно указал).

### Шаг 3. Бэкенд обращается к NLP-модулю
- Отправляет текст в Python-сервис.
- Получает JSON-схему с фильтрами (как мы обсуждали ранее).

**Если NLP не отвечает (таймаут):** Используется **fallback-режим** — простой парсер на регулярках внутри Go-сервиса (без ML). Это гарантирует, что поиск сработает даже при сбоях Python.

### Шаг 4. Бэкенд обращается к Geo-движку
- Передает JSON-фильтры в свой же внутренний модуль (на Go).
- Geo-движок строит SQL-запрос, выполняет его в PostGIS.

### Шаг 5. Пост-обработка результатов
- Бэкенд **ранжирует** результаты по Workability Score (для запросов типа "работа") или по рейтингу/расстоянию для остальных.
- Добавляет **человеческие объяснения** к каждому месту:
  - *"Здесь есть Wi-Fi, розетки и тихо — отлично для работы"*
  - *"Терраса открыта, можно посидеть на свежем воздухе"*

### Шаг 6. Кеширование
- Если запрос повторяется (в течение 5 минут) — результат берется из Redis, без обращения к БД и NLP.

### Шаг 7. Ответ пользователю
- **Веб:** Отображает карточки мест на карте (интеграция с Leaflet/OpenStreetMap).
- **Telegram:** Присылает список с кнопками "Показать на карте" и "Построить маршрут".

---

## 4. КЛЮЧЕВЫЕ ФИЧИ ДЛЯ B2C (ЧТО ДЕЛАЕТ ПРОДУКТ УДОБНЫМ)

### 4.1. Интеллектуальные подсказки (автокомплит)
Пока пользователь печатает, система предлагает готовые шаблоны:
- *"кафе с... [террасой] [вайфаем] [завтраками]"*
- *"ресторан где... [недорого] [с детской комнатой]"*

Это снижает порог входа — пользователю не нужно придумывать запрос, можно выбрать из списка.

### 4.2. Геолокация по умолчанию
Если пользователь зашел с мобильного устройства — система автоматически определяет его местоположение (через браузерный API) и ищет рядом.  
Если геолокация недоступна — использует IP-адрес (приблизительно).  
Пользователь может вручную указать адрес или метро.

### 4.3. История запросов (для каждого пользователя)
Сохраняем последние 20 запросов в **LocalStorage браузера** или в Telegram-сессии.  
Пользователь может быстро повторить поиск (например, *"Где я завтракал в прошлую субботу?"*).

**Важно:** Не храним персональные данные на сервере — только на клиенте (GDPR-безопасно).

### 4.4. Фильтры "быстрого доступа" (чипсы)
Под полем ввода висят кнопки-фильтры:
- ☕ Кофе
- 🍳 Завтрак
- 💼 Работа
- 🌿 Терраса
- 🍷 Ужин

При клике на чипс запрос автоматически дополняется (например, *"кафе с террасой"*).

### 4.5. "Почему именно это место?" (объяснимость AI)
Под каждым результатом — короткое объяснение:
- *"Мы выбрали это место, потому что у него высокий рейтинг (4.8), есть Wi-Fi и оно открыто до 23:00"*
- *"Рядом с метро (300 м), есть терраса и тихо — идеально для работы"*

Это повышает доверие пользователя к системе.

### 4.6. Поделиться результатом
Кнопка *"Поделиться"* генерирует ссылку с предзаполненным запросом (например, `https://ваш-сайт.com/search?q=кафе+с+террасой+метро+Парк+Культуры`).  
Пользователь может отправить ссылку другу.

### 4.7. Избранное (сохраненные места)
Пользователь может сохранять понравившиеся места в "Избранное" (хранится в браузере или в Telegram).  
Потом легко найти сохраненное место, не вводя запрос заново.

---

## 5. КАК РАБОТАЕТ РАНЖИРОВАНИЕ (АЛГОРИТМ БЕЗ ДЕНЕГ)

Поскольку мы убрали платные API, ранжирование становится **чисто релевантным**. Формула зависит от типа запроса:

### 5.1. Для запросов "работа" (intent = work)
```
Итоговый балл = 
  (Наличие Wi-Fi * 3) + 
  (Наличие розеток * 3) + 
  (Тишина * 2) + 
  (Рейтинг / 5 * 2) + 
  (1 / Расстояние * 100) - 
  (Наличие живой музыки * 3)
```
Чем выше балл — тем выше место в выдаче.

### 5.2. Для запросов "завтрак" (intent = breakfast)
```
Итоговый балл = 
  (Наличие завтраков * 5) + 
  (Рейтинг / 5 * 3) + 
  (Цена < среднего * 2) + 
  (1 / Расстояние * 50)
```

### 5.3. Для запросов "романтический ужин" (intent = dinner)
```
Итоговый балл = 
  (Наличие свечей/вина * 4) + 
  (Вид из окна * 4) + 
  (Рейтинг / 5 * 2)
```

### 5.4. Для общих запросов (intent = default)
```
Итоговый балл = 
  (Рейтинг / 5 * 5) + 
  (1 / Расстояние * 50) + 
  (Количество отзывов / 100 * 2)
```

**Важно:** Пользователь может переключать сортировку вручную (по расстоянию, по рейтингу, по популярности).

---

## 6. КАК ПОПОЛНЯЮТСЯ ДАННЫЕ (БЕЗ ПОЛЬЗОВАТЕЛЕЙ)

### 6.1. Источники данных (бесплатные)
- **OpenStreetMap:** Основной источник POI (кафе, рестораны, бары).
- **Википедия:** Для дополнительной информации о достопримечательностях.
- **Ручное обогащение:** Администратор добавляет фото, часы работы, контакты через админ-панель.

### 6.2. Периодичность обновления
- OSM-дамп — раз в неделю (автоматически).
- Ручные правки — моментально (через админку).

### 6.3. Обогащение тегами (автоматическое)
Для POI, у которых нет тегов (например, нет тега `terrace`), система пытается **догадаться**:
- Если в названии есть *"Летняя"* или *"Веранда"* → ставим `terrace = yes`.
- Если в отзывах часто упоминается *"шумно"* → ставим `noise_level = high`.

**Это делается раз в неделю фоновым скриптом.**

### 6.4. Обратная связь от пользователей
Добавляем кнопку *"Это место закрылось"* или *"Нет террасы"*.  
Если 5+ пользователей пожаловались — система автоматически убирает тег или метку.

---

## 7. ИНТЕРФЕЙСЫ (КАК ПОЛЬЗОВАТЕЛЬ ВЗАИМОДЕЙСТВУЕТ)

### 7.1. Веб-версия (основной канал)
- **Главная страница:** Большое поле поиска с подсказками.
- **Результаты:** Список карточек (фото, название, расстояние, рейтинг, теги) + карта с маркерами.
- **Детальная карточка:** При клике на место — полная информация (адрес, телефон, часы работы, ссылка на сайт, маршрут).

### 7.2. Telegram-бот (для мобильных пользователей)
- **Простота:** Пользователь пишет запрос, бот присылает 5 лучших вариантов.
- **Интерактивность:** Кнопки *"Показать еще"*, *"Построить маршрут"* (открывает Яндекс.Карты/Navigation), *"Сохранить"*.
- **Уведомления:** Бот может присылать рекомендации (*"Сегодня хорошая погода, вот кафе с террасой рядом с вами"*).

### 7.3. Мобильное приложение (в будущем)
- На React Native или Flutter.
- Основная фишка — **голосовой ввод** (*"Найди, где поесть рядом"*).

---

## 8. БЕЗОПАСНОСТЬ И ЗАЩИТА (БЕЗ АВТОРИЗАЦИИ)

### 8.1. Защита от спама (Rate Limiting)
Ограничиваем количество запросов с одного IP-адреса:
- Не более 30 запросов в минуту.
- Не более 500 запросов в день.

Если лимит превышен — возвращаем ошибку *"Слишком много запросов. Попробуйте через минуту"*.

### 8.2. Защита от инъекций
- Все пользовательские данные экранируются перед отправкой в SQL.
- Используем параметризованные запросы (никакой конкатенации строк).

### 8.3. Фильтрация контента
- Если пользователь вводит нецензурную лексику или опасные запросы (*"как взломать"*) — система игнорирует запрос и показывает вежливую ошибку.

---

## 9. МЕТРИКИ УСПЕХА (ЧТО ОТСЛЕЖИВАТЬ)

| Метрика | Целевое значение | Как измеряем |
| :--- | :--- | :--- |
| **Время ответа** | < 500 мс (p95) | Логи Go-сервиса |
| **Точность NLP** | > 85% | Ручная проверка 100 случайных запросов |
| **CTR (кликабельность)** | > 40% | Аналитика (сколько раз пользователь кликнул на место) |
| **Повторные визиты** | > 30% пользователей возвращаются через неделю | Cookies / Telegram-сессии |
| **Доля запросов с геолокацией** | > 60% | Логи (если пользователь разрешил доступ к гео) |
| **Средняя длина запроса** | 5-7 слов | Аналитика текстов |

---

## 10. ДОРОЖНАЯ КАРТА (ЧТО ДЕЛАТЬ ПОШАГОВО)

### Этап 1. Ядро (2 недели)
- Загрузить OSM-дамп в PostGIS (только нужные категории).
- Написать NLP на Python (словари + регулярки, без ML).
- Сделать Go-бэкенд с одним эндпоинтом `/search`.
- Протестировать на 20-30 ручных запросах.

### Этап 2. Веб-интерфейс (1 неделя)
- Сделать простую HTML-страницу с полем ввода и картой (Leaflet).
- Подключить к бэкенду через AJAX.
- Добавить автокомплит (список популярных запросов).

### Этап 3. Telegram-бот (1 неделя)
- Написать бота на Python (через python-telegram-bot).
- Подключить к тому же бэкенду (Go).
- Добавить кнопки "Показать еще", "Маршрут".

### Этап 4. Обогащение и улучшения (постоянно)
- Собирать обратную связь от пользователей (кнопки *"Полезно"* / *"Бесполезно"*).
- Дорабатывать NLP-словари на основе реальных запросов.
- Добавить фото мест (из Википедии или через поиск изображений).

---

## 11. ПОЧЕМУ B2C — ЭТО ХОРОШО (ПРЕИМУЩЕСТВА)

| Критерий | B2B (с монетизацией) | B2C (бесплатно) |
| :--- | :--- | :--- |
| **Сложность запуска** | Высокая (нужна биллинг-система) | Низкая (достаточно бэкенда и фронтенда) |
| **Пользователи** | Мало (только бизнесы) | Много (все жители города) |
| **Обратная связь** | Медленная (от менеджеров) | Быстрая (пользователи пишут в Telegram) |
| **Монетизация** | Прямая ($ за запрос) | Косвенная (реклама, партнерство) |
| **Юридические риски** | Высокие (договоры, налоги) | Низкие (просто сервис) |
| **Масштабируемость** | Ограничена бюджетом клиентов | Ограничена только вашим сервером |

**Стратегия:** Сначала запускаете B2C, наращиваете аудиторию (10-50 тысяч пользователей), а потом вводите **премиум-функции** (например, *"Показать места с самыми высокими оценками"* за небольшую плату) или рекламные интеграции.

---

## 12. ПРИМЕР РАБОТЫ (СКВОЗНОЙ СЦЕНАРИЙ)

1. **Пользователь** открывает сайт и пишет: *"хочу тихое кафе с террасой недалеко от метро Парк Культуры"*.

2. **Бэкенд** принимает запрос, определяет город (по IP — Москва).

3. **NLP** парсит:
   - Категория: кафе.
   - Теги: терраса = да, тихо = да.
   - Метро: Парк Культуры.
   - Радиус: "недалеко" → 300 метров.

4. **Geo-движок** находит 12 кафе в радиусе 300 м от метро, исключает те, у которых нет террасы (остается 7), ранжирует по Workability Score.

5. **Ответ** (пример):
   ```
   1. Кофейня "Сады" (расстояние 240 м)
      ⭐ 4.8 | Wi-Fi есть | Розетки есть | Тишина
      "Отличное место для работы: быстрый Wi-Fi, удобные розетки и очень тихо"
   
   2. Кафе "Летняя веранда" (расстояние 180 м)
      ⭐ 4.6 | Wi-Fi есть | Терраса
      "Красивая терраса с видом на парк, но может быть шумно в выходные"
   ```

6. **Пользователь** кликает на первое место, видит полную карточку (адрес, телефон, часы работы), строит маршрут.

7. Через неделю пользователь снова открывает сайт и видит в истории: *"хочу тихое кафе с террасой недалеко от метро Парк Культуры"* — может повторить поиск одним кликом.

---

## 13. ЧТО ДАЛЬШЕ (РАЗВИТИЕ ПРОДУКТА)

### Через 3 месяца после запуска:
- **Добавить голосовой поиск** (через Web Speech API).
- **Интеграция с каршерингом:** показать не только кафе, но и парковки рядом.
- **Персонализация:** если пользователь часто ищет веганские места — поднимать их в выдаче выше.
- **Мультиязычность:** поддержка английского (для туристов).

### Через 6 месяцев:
- **Мобильное приложение** с push-уведомлениями (*"Сегодня в вашем любимом кафе новый десерт"*).
- **Система лояльности:** пользователи получают бонусы за то, что делятся ссылками.
- **Заработок:** размещение рекламных карточек (CPM-модель).

---

## ИТОГ

Теперь у вас **чистый B2C-продукт**:

- ✅ Без монетизации (на старте).
- ✅ Без API-ключей и биллинга.
- ✅ Только одна задача: **помочь пользователю найти идеальное место**.
- ✅ Быстрый запуск (2-3 недели до первого рабочего прототипа).
- ✅ Потенциал для роста (добавление новых фич и монетизация через рекламу).

**Главное отличие от конкурентов:** вы даете пользователю **готовые решения**, а не просто фильтры. Это как ChatGPT для поиска мест — написал запрос, получил персонализированный ответ.

Готовы детализировать любой блок (например, как именно строить карту или как делать автокомплит)? Просто скажите.